# Domain Layer
Contains core business logic, entity models, use cases, and repository interfaces.
- `model/`: Pure Kotlin data models (Session, TelemetryData, VideoMetadata)
- `usecase/`: Business use cases (StartRecordingUseCase, CalculateGForceUseCase, etc.)
- `repository/`: Repository interfaces
